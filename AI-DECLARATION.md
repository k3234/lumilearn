# AI 使用声明（AI-DECLARATION）

> 本文件声明 LumiLearn 项目开发过程中 AI 工具的参与方式、我的角色与责任边界。
> 适用于 GOAI 无界应用赛道参赛作品《LumiLearn — 本地优先 AI 教育智能体》。
> 日期：2026-08-12

---

## 一、我的角色

本项目由我（一名高中生开发者）主导，在 AI 工具的深度辅助下完成。我的核心工作包括：

| 角色 | 具体内容 |
|---|---|
| **系统架构设计** | 确定整体架构（本地优先 + 费曼教学 + 多 Agent 协作 + RAG 知识库），划分模块边界（framework / goai_web / teacher_portal / admin），设计端口与服务拓扑 |
| **任务拆解** | 依据 GOAI 行动规划将开发拆分为 Day1–Day6 任务（基础加固 → 多 Agent → RAG → 演示素材 → 集成打磨 → 提交），逐日验收 |
| **技术决策** | 选择技术路线：CPU 可跑（自研 8M 模型 + Qwen2.5 LoRA 微调 + Ollama 部署）、不引入向量数据库（关键词倒排索引 RAG）、三端口 + 多端口扩展 |
| **代码审查** | 对 AI 生成的代码逐项 review：验证功能、检查安全（无凭据泄漏）、确认与既有架构一致，测试通过后才合入 |
| **验收标准制定** | 为每个任务定义通过标准（如多 Agent 26 项测试、任务三 56 项测试、全产品 124 项回归、天虹真实服务 36/37 项测评），驱动开发闭环 |

## 二、使用的 AI 工具及其用途

| 工具 | 用途 |
|---|---|
| **Trae CN（AI IDE）** | 项目开发主环境：代码编写、重构、调试、版本管理（git）的日常开发 |
| **Trae Work CN** | 复杂任务编排与自动化：多 Agent 协作系统开发、远程服务器（天虹）部署、测评脚本执行、数据训练流水线 |

具体 AI 辅助场景：

- **从零训练模型**：设计 8M 参数 Transformer 架构、BPE tokenizer、训练/评测脚本（CPU 训练）
- **LoRA 微调**：Qwen2.5-1.5B 微调流程（数据构建、CPU 训练、GGUF 转换、Ollama 部署）
- **费曼教学引擎**：五步教学法编排、交互式单步引导、输出检测五维评分
- **多 Agent 协作系统**：FeynmanTeacher → ScoreAgent → CoachAgent 串行编排与失败降级
- **RAG 知识库**：关键词倒排索引检索服务、与教学 Agent 集成、检索 API
- **前端页面**：Admin 管理面板、教师门户、学生端、分析仪表盘
- **数据合规**：账号权限分级、数据范围隔离、导出审批流

## 三、AI 生成代码的审核方式

1. **功能验证**：每项功能完成后运行测试套件（pytest 核心 59 项、多 Agent 26 项、任务三 56 项、全产品回归 124 项），全部通过才合入。
2. **真实环境测评**：部署到天虹服务器（CPU 环境）后执行在线测评（RAG 检索、多 Agent 真实推理、权限管理、导出审批），验证线上行为。
3. **安全审查**：每次提交前扫描仓库，确认无真实 IP、SSH 密码、API Key 等敏感信息；凭据一律使用环境变量。
4. **架构一致性**：新增代码遵循既有分层（framework/services、framework/api/routes、独立端口应用），不引入破坏性改动；CI（GitHub Actions）守护主分支。

## 四、责任声明

- 我对全部代码的**功能正确性**负责：每个功能均经测试与真实环境验证。
- 我对全部代码的**可维护性**负责：代码遵循统一分层、命名规范、文档同步（README / CHANGELOG / 设计文档）。
- AI 工具作为**辅助手段**参与编码，但架构决策、验收标准、最终合入由我把控。
- 若代码存在缺陷，责任由我承担，而非归咎于 AI 工具。

## 五、开发过程中使用的技能（Skills）与插件（Plugins）

| 类型 | 名称 | 用途 |
|---|---|---|
| 技能 | `superpowers` | 项目工作流编排（lumilearn-workflow：计划 → 执行 → 验证） |
| 技能 | `understand-anything` | 代码库理解，将 AI 生成代码转化为知识图谱辅助审查 |
| 技能 | `web-app-development` | 网页应用设计、开发与调试 |
| 技能 | `frontend-design` | 前端页面与界面设计（Admin / 教师端 / 学习页） |
| 技能 | `rapid-prototype-craft` | 学生端学习平台原型快速开发 |
| 技能 | `data-processing` | 训练数据清洗、转换、统计 |
| 插件 | Trae 内置扩展 | 浏览器控制（演示验证）、GitHub（版本管理）、Screenshot（页面验证）等 |

> 说明：部分技能/插件按需加载，核心开发流程（编码、测试、部署）以 Trae CN / Trae Work CN 为统一入口。

---

*本声明随项目代码一并提交，保证透明、可核查。*
