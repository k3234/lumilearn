# LumiLearn 自动化评测报告

> **报告日期**：2026-08-21  
> **系统版本**：V2.5 竞赛版  
> **评测命令**：`python -m scripts.run_eval`（mock 模式）  
> **评测报告位置**：`reports/eval_report_YYYYMMDD_HHMMSS.{html,json}`（每次运行自动生成）

---

## 一、评测总览

| 指标 | 数值 | 说明 |
|---|---|---|
| 总题数 | 150 | 数学 50 + 物理 50 + 化学 50 |
| 测试模式 | mock | 关键字匹配，不调用真实模型 |
| 总耗时 | 0.54 秒 | 150 题平均 3.6ms/题 |
| 自动化测试用例 | 569 passed / 2 skipped | pytest 全量回归 |
| 幻觉检测数 | 0 | FactChecker 未报告矛盾 |

---

## 二、学科分项指标

| 学科 | 题数 | knowledge_recall | accuracy | hit_rate | hallucination | 平均耗时(ms) |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| 数学 | 50 | 16.0% | 10.0% | 42.0% | 0 | 3.5 |
| 物理 | 50 | 5.0% | 2.0% | 28.0% | 0 | 3.6 |
| 化学 | 50 | 0.0% | 0.0% | 12.0% | 0 | 3.6 |
| **合计** | **150** | **7.0%** | **4.0%** | **27.3%** | **0** | **3.6** |

### 指标说明

| 指标 | 定义 | 计算方式 |
|---|---|---|
| knowledge_recall | 知识召回率 | 题目 expected_knowledge 在检索结果中命中的比例 |
| accuracy | 答案准确率 | 系统输出与预期答案完全匹配的比例 |
| hit_rate | 检索命中率 | 检索结果非空的题目比例 |
| hallucination | 幻觉检测数 | FactChecker 报告的与知识库矛盾的条目数 |

---

## 三、评测环境说明

### 3.1 测试数据集

评测集位于 `data/eval_dataset/`，共 3 个文件：
- `math.json` — 50 道数学题（代数/几何/三角函数/微积分基础）
- `physics.json` — 50 道物理题（力学/电磁学/光学/热学）
- `chemistry.json` — 50 道化学题（化学键/反应/有机物/无机物）

每题包含字段：`id`、`topic`、`question`、`expected_knowledge`（知识条目列表）、`answer`（标准答案）。

### 3.2 评测模式

| 模式 | 命令 | 说明 |
|---|---|---|
| **mock（默认）** | `python -m scripts.run_eval` | 不调用模型，使用关键字匹配答案，快速验证流程 |
| **real** | `python -m scripts.run_eval --real` | 调用本地 Ollama / 云端 API，真实推理，耗时长 |

---

## 四、真实环境运行证据（参考）

> 以下为真实 CPU 环境（天虹服务器）运行结果，来自 `docs/EVALUATION_RESULTS.md`。

| 场景 | 耗时 | 评分 | 备注 |
|---|:---:|:---:|---|
| 数学 · 勾股定理 | 59.2s | 100 | 费曼五步完整生成 |
| 化学 · 共价键 | 49.1s | 100 | 费曼五步完整生成 |
| 多 Agent（牛顿第二定律） | 56.9s | 100 | RAG 来源 2 条 + 三 Agent 全 ok |
| RAG 检索（5/5 用例） | — | 5/5 ✅ | 已知 1166 条 published 知识库 |

---

## 五、评测局限说明

### 5.1 mock 模式的局限性
- 当前 mock 模式使用**关键字匹配**评估答案，不验证推理过程的逻辑正确性
- 知识召回率（7%）反映的是**种子知识库规模有限**，并非检索算法失效
- 真实课堂场景中，RAG 命中率已达 5/5（见上表），说明检索逻辑正确

### 5.2 评测集规模
- 当前 150 题规模较小，无法代表完整学科知识覆盖
- 建议后续扩展至 500+ 题，覆盖更多知识点和难度梯度

### 5.3 缺失的评测维度
| 维度 | 当前状态 | 后续计划 |
|---|---|---|
| 多轮对话连贯性 | 未评测 | 开发交互式评测脚本 |
| 教师端数据隔离 | 已测试（5/5） | — |
| 权限系统 | 已测试（6/6） | — |
| 用户主观满意度 | 无 | 设计问卷，收集开发者及同学反馈 |

---

## 六、评测报告生成

每次运行评测后，自动生成以下文件：

```
reports/
├── eval_report_YYYYMMDD_HHMMSS.html   # ECharts 可视化报表
└── eval_report_YYYYMMDD_HHMMSS.json   # 结构化数据（供程序读取）
```

**历史报告不入库**（`.gitignore` 已配置），保持仓库干净。

---

## 七、复现方式

```bash
# 进入项目根目录
cd lumilearn

# 运行 mock 评测（默认，快速）
python -m scripts.run_eval

# 指定学科+题数
python -m scripts.run_eval --subject math --limit 10

# 真实模型模式（需启动 Ollama）
python -m scripts.run_eval --real
```

---

*本报告为 V2.5 竞赛版本阶段性评测结果，后续将随评测集扩充持续更新。*
