[pytest]
; 全量测试默认只收集 tests/ 目录，避免误收集 scripts/_*_test.py 等独立脚本
testpaths = tests
norecursedirs = scripts docs deploy prototypes static skills .git .venv venv node_modules __pycache__ .pytest_cache
; 单测试超时 240s 时 dump 所有线程栈并退出（用于定位挂起测试）
faulthandler_timeout = 240
faulthandler_exit_on_timeout = true
